// @ts-nocheck — Remotion is compiled via `pnpm remotion:*`, not Next; core typings clash with Next’s checker.
import { registerRoot } from "remotion";

import { RemotionRoot } from "./Root";

registerRoot(RemotionRoot);
