// @ts-nocheck — Remotion bundle only.
import { PainScene } from "./scenes/PainScene";

/** Placeholder composition — extend with additional scenes as needed. */
export function LaunchVideo() {
  return <PainScene />;
}
