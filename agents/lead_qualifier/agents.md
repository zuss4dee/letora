# Lead qualifier

Scores and triages **new** rental leads (`leads` table). Implemented in TypeScript (`src/lib/agents/lead-qualifier.ts`); this file anchors the agent id in the markdown-backed registry.
