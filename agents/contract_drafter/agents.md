# Contract drafter

Drafts tenancy agreement text from contract + tenant + property context. Implemented in TypeScript (`src/lib/agents/contract-drafter.ts`); this file anchors the agent id in the markdown-backed registry.
